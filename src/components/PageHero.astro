---
type PageHeroVariant = "collection" | "artist" | "packages" | "models" | "service" | "studio";

interface Props {
  title: string;
  lead?: string;
  subtitle?: string;
  headingId: string;
  variant: PageHeroVariant;
  eyebrow?: string;
  description?: string | string[];
  className?: string;
}

const { title, lead, subtitle, headingId, variant, eyebrow, description, className } = Astro.props;
const descriptions = description ? (Array.isArray(description) ? description : [description]) : [];
---

<section class:list={["page-hero", `page-hero--${variant}`, className]} aria-labelledby={headingId}>
  {eyebrow && <p class="page-hero__eyebrow">{eyebrow}</p>}
  <h1 id={headingId}>{title}</h1>
  {subtitle && <p class="page-hero__subtitle">{subtitle}</p>}
  {lead && <p class="page-hero__subtitle page-hero__lead">{lead}</p>}
  {descriptions.length > 0 && <div class="page-hero__description">{descriptions.map((paragraph) => <p>{paragraph}</p>)}</div>}
  <slot />
</section>
