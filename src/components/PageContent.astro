---
import { faqItemsFor, introFor, site, localizedNav } from "../lib/site.mjs";
import { homeContent, studioContent, studioImages, studioGalleryImages, studioBackdropImages, selfieContent, selfieImages, equipmentContent, equipmentImages, propsContent, propsImages, residentArtistContent, residentArtistImages, modelsContent, modelImages, packagesContent, packagesImages, commonPhotoPackages, commonUpgrades, serviceContent, serviceImages, specialContent, specialImages, mentoringContent, homeImages } from "../data/content.mjs";
import BookingFooter from "./BookingFooter.astro";

const { page } = Astro.props;
const hu = page.language === "hu";
const home = homeContent[hu ? "hu" : "en"];
const studio = studioContent[hu ? "hu" : "en"];
const selfie = selfieContent[hu ? "hu" : "en"];
const isEquipment = page.path === "/equipment" || page.path === "/hu/equipment";
const isProps = page.path === "/props" || page.path === "/hu/props";
const collectionPage = isEquipment ? equipmentContent[hu ? "hu" : "en"] : isProps ? propsContent[hu ? "hu" : "en"] : null;
const collectionImages = isEquipment ? equipmentImages : propsImages;
const collectionImageMap = collectionImages as Record<string, Array<{ src: string; alt: string; width?: number; height?: number }>>;
const collectionSections = collectionPage?.sections.map((section) => ({
  heading: section.heading,
  subheading: "subheading" in section ? section.subheading : "",
  paragraphs: section.paragraphs,
  galleries: section.galleries.map((gallery) => ({ imageKey: gallery.imageKey, note: ("note" in gallery ? gallery.note : null) as string[] | null }))
})) ?? [];
const artistType = page.path.includes("photographers-budapest") ? "photographers" : page.path.includes("/stylists") ? "stylists" : page.path.includes("make-up-artists") ? "makeup" : null;
const artistPage = artistType ? residentArtistContent[artistType][hu ? "hu" : "en"] : null;
const artistImages = artistType ? residentArtistImages[artistType] : null;
const artistPageAny = artistPage as any;
const artistImagesAny = artistImages as any;
const artistProfiles = artistPageAny?.profiles.map((profile: any) => {
  const source = artistType === "makeup" ? artistImagesAny.profiles[profile.imageIndex] : { image: artistImagesAny[profile.image], gallery: artistImagesAny[profile.gallery] };
  return { ...profile, image: source.image, gallery: source.gallery };
}) ?? [];
const isModels = page.path === "/models-budapest" || page.path === "/hu/models-budapest";
const modelPage = isModels ? modelsContent[hu ? "hu" : "en"] : null;
const modelPageAny = modelPage as any;
const modelPeopleImages = modelImages.people;
const isPackages = page.path === "/packages" || page.path === "/hu/packages";
const packagesPage = isPackages ? packagesContent[hu ? "hu" : "en"] : null;
const packagesPageAny = packagesPage as any;
const serviceType = page.path.includes("commercial-photography") ? "commercial" : page.path.includes("corporate-photography") ? "corporate" : page.path.includes("portrait-photography") ? "portrait" : page.path === "/fitness" || page.path === "/hu/fitness" ? "fitness" : null;
const servicePage = serviceType ? serviceContent[serviceType][hu ? "hu" : "en"] : null;
const servicePageAny = servicePage as any;
const serviceImagesAny: any = serviceType ? (serviceImages as any)[serviceType] : null;
const fitnessAudienceImages: any = serviceImages.fitnessAudience;
const specialType = page.path.includes("glamour-boudoir") ? "glamour" : page.path.includes("model-polaroids") ? "models" : page.path.includes("pet-photography") ? "pet" : null;
const specialPage = specialType ? (specialContent as any)[specialType][hu ? "hu" : "en"] : null;
const specialPageAny = specialPage as any;
const specialImagesAny: any = specialType ? (specialImages as any)[specialType] : null;
const isIdPhoto = page.path === "/id-photo" || page.path === "/hu/id-photo";
const isMentoring = page.path === "/photographer-mentoring-budapest" || page.path === "/hu/photographer-mentoring-budapest";
const mentoringPage: any = isMentoring ? mentoringContent[hu ? "hu" : "en"] : null;
const bookingExtras = hu ? [["Papír háttér", "csak ha ráléptek", "3000 Ft"], ["Smink", "profi sminkeseinktől", "15.000.Ft-tól"], ["Nagy Angyal Szárnyak", "fehér, vagy fekete / 1m+", "8.000.Ft"], ["Asszisztencia", "segítünk magyarul, vagy angolul", "4.000.Ft / óra"], ["Styling", "dolgozz profi stylistjainkkal", "35.000.Ft-tól"], ["Különleges Ruhák", "Menyasszonyi ruha, Népviselet, stb...", "5000Ft-tól"], ["Kamera bérlés", "analóg vagy digitális kamerák és filmek", "4000-9000 Ft"], ["Fotómodellek", "tapasztalt modellek", "15.000.Ft / órától"], ["Füstgép", "folyadékkal együtt", "5000Ft"], ["Akvárium", "bele fér egy ember - vízzel töltve", "39.000.Ft"]] : [["Paper Backdrop", "applies if you step on it", "3.000. HUF"], ["Make-up", "work with our pro make-up artists", "from 15.000. HUF"], ["Large Angel Wings", "black or white / 1m+", "8.000. HUF"], ["Assistance", "we help you in Hungarian or English", "4.000. HUF / hour"], ["Styling", "work with our pro stylist", "from 35.000. HUF"], ["Unique Outfits", "Bridal Dress, Folk Dress, etc.", "from 5.000. HUF"], ["Camera Rental", "analog or digital cameras and films", "4.000.-9.000. HUF"], ["Photo Models", "work with our pro models", "from 15.000. HUF / hour"], ["Fog Machine", "with fog juice included", "5.000. HUF"], ["Aquarium", "fits a human - filled with water", "39.000. HUF"]];
const servicePackages = serviceType === "portrait" ? commonPhotoPackages[hu ? "hu" : "en"] : servicePageAny?.packages ?? [];
const serviceUpgrades = commonUpgrades[hu ? "hu" : "en"];
const serviceLinks = localizedNav(page.language).flatMap((group) => group.links)
  .filter(([path]) => path !== page.path)
  .slice(0, 4);
const relatedHref = hu ? "/hu/packages" : "/packages";
---

{page.section === "home" ? (
  <>
    <section class="home-hero" aria-labelledby="home-title">
      <div class="home-hero__image-wrap">
        <img class="home-hero__image" src={homeImages.hero.src} alt={homeImages.hero.alt} width={homeImages.hero.width} height={homeImages.hero.height} fetchpriority="high" decoding="async" />
        <img class="home-hero__icon" src={homeImages.icon.src} alt={homeImages.icon.alt} width="450" height="436" decoding="async" />
        <div class="home-hero__copy">
          <h1 id="home-title">{page.h1}</h1>
          <a class="button button--inverted" data-gtag-event="booking_click" data-gtag-location="home" href={site.bookingUrl}><span class="home-hero__cta-desktop">{hu ? "foglalás" : "book the studio"}</span><span class="home-hero__cta-mobile">{hu ? "foglalás" : "book the photostudio"}</span></a>
        </div>
      </div>
    </section>
    <section class="home-favourite" aria-labelledby="favourite-title">
      <div class="home-favourite__intro">
        <h2 id="favourite-title">{hu ? <>az új kedvenc<br class="home-mobile-break" />fotóstúdiód</> : <>your new favourite<br class="home-mobile-break" />photostudio</>}</h2>
        <p>{home.introHeading}</p>
      </div>
      <div class="home-feature-grid">
        {home.features.map(([title, copy, href, label], index) => <article class="home-feature">
          <h3>{title}</h3>
          <img src={homeImages.features[index]?.src} alt={homeImages.features[index]?.alt} width="181" height="181" loading="lazy" decoding="async" />
          <p>{copy}</p>
          <a class="text-link" href={href}>{label}</a>
        </article>)}
      </div>
    </section>
    <section class="studio-showcase" aria-labelledby="studio-showcase-title">
      <div class="studio-showcase__copy">
        <h2 id="studio-showcase-title">{home.studioShowcaseTitle}</h2>
        <p>{home.studioShowcaseIntro}</p>
        <p>{home.studioShowcaseBody}</p>
        <a class="button button--secondary studio-showcase__more" href={hu ? "/hu/studio" : "/studio"}>{home.studioShowcaseCta}</a>
      </div>
      <div class="studio-showcase__visuals">
        <div class="studio-showcase__gallery" aria-label={hu ? "Képgaléria a stúdióról" : "Photo gallery of the studio"}>
          {homeImages.creatingArt.map((image, index) => <button class="studio-showcase__gallery-item" type="button" data-gallery-image data-gallery-index={index} data-gallery-src={image.src} data-gallery-alt={image.alt} aria-label={hu ? `Kép megnyitása: ${index + 1}` : `Open image ${index + 1}`}>
            <img src={image.src} alt={image.alt} width={image.width} height={image.height} loading="lazy" decoding="async" />
          </button>)}
        </div>
        <aside class="studio-showcase__rent">
          <h3>{hu ? "Stúdióbérlés" : "Studio Rent"}</h3>
          <p>{hu ? <>7.000 HUF<br />/ órától</> : <>starting from<br />7.000. HUF / hour</>}</p>
          <a class="button button--dark studio-showcase__book" data-gtag-event="booking_click" data-gtag-location="home-studio-showcase" href={site.bookingUrl}>{hu ? "foglald le a stúdiót" : "book the photostudio"}</a>
        </aside>
      </div>
    </section>
    <dialog class="studio-lightbox" aria-label={hu ? "Stúdió képgaléria" : "Studio photo gallery"} data-studio-lightbox>
      <button class="studio-lightbox__close" type="button" aria-label={hu ? "Galéria bezárása" : "Close gallery"} data-lightbox-close>×</button>
      <button class="studio-lightbox__previous" type="button" aria-label={hu ? "Előző kép" : "Previous image"} data-lightbox-previous>←</button>
      <img data-lightbox-image src={homeImages.creatingArt[0].src} alt={homeImages.creatingArt[0].alt} width={homeImages.creatingArt[0].width} height={homeImages.creatingArt[0].height} />
      <button class="studio-lightbox__next" type="button" aria-label={hu ? "Következő kép" : "Next image"} data-lightbox-next>→</button>
    </dialog>
    <section class="beginner-section" aria-labelledby="beginner-title">
      <div class="beginner-section__main">
        <h2 id="beginner-title">{home.beginnerHeading}</h2>
        <p class="beginner-section__lead">{home.beginnerLead}</p>
        <div class="beginner-section__proof">{home.beginnerProof.map((paragraph, index) => <p>{index === 1 ? <strong>{paragraph}</strong> : paragraph}</p>)}</div>
        <ul class="beginner-section__steps">{home.beginnerSteps.map((step) => <li>{step}</li>)}</ul>
        <p class="beginner-section__cta"><strong>{home.beginnerCtaLead}</strong>{home.beginnerCtaTail}</p>
      </div>
      <div class="beginner-section__testimonials">
        <blockquote><p>{home.beginnerQuote}</p><footer>{home.beginnerAttribution}</footer></blockquote>
        <blockquote><p>{home.beginnerQuoteSecond}</p><footer>{home.beginnerAttributionSecond}</footer></blockquote>
      </div>
    </section>
    <section class="home-artists" aria-labelledby="artists-title">
      <div class="home-artists__intro">
        <h2 id="artists-title">{home.artistsHeading}</h2>
        <p>{home.artistsLead}</p>
      </div>
      <div class="home-artists__grid">{home.artists.map(([title, copy, href, label], index) => <article class="home-artist-card">
        <h3>{title}</h3>
        <img src={homeImages.artists[index].src} alt={hu ? homeImages.artists[index].altHu : homeImages.artists[index].alt} width={homeImages.artists[index].width} height={homeImages.artists[index].height} loading="lazy" decoding="async" />
        <p>{copy}</p>
        <a class="text-link" href={href}>{label}</a>
      </article>)}</div>
    </section>
    <section class="home-services" aria-labelledby="services-title">
      <div class="home-services__intro">
        <h2 id="services-title">{home.photographersHeading}</h2>
        <p>{home.photographersCopy}</p>
      </div>
      <div class="home-services__grid">{home.photographyServices.map(([title, href, label], index) => <article class="home-service-card">
        <h3>{title}</h3>
        <img src={homeImages.photographers[index].src} alt={homeImages.photographers[index].alt} width={homeImages.photographers[index].width} height={homeImages.photographers[index].height} loading="lazy" decoding="async" />
        <a class="text-link" href={href}>{label}</a>
      </article>)}</div>
      {hu ? <p class="home-services__packages">{home.packagesCopy}</p> : <a class="home-services__packages" href="/photographers-budapest">{home.packagesCopy}</a>}
    </section>
  </>
) : page.path === "/studio" || page.path === "/hu/studio" ? (
  <>
    <section class="studio-intro" aria-labelledby="studio-title">
      <div class="studio-intro__inner">
        <h1 id="studio-title">{page.h1}</h1>
        <p class="studio-intro__eyebrow">{studio.eyebrow}</p>
        <div class="studio-intro__copy">
          <p>{studio.paragraphs[0]}</p>
          <p>{studio.paragraphs[1]}</p>
        </div>
      </div>
    </section>
    <section class="studio-overview" aria-labelledby="studio-facts-title">
      <div class="studio-overview__floorplan"><img src={studioImages[0].src} alt={hu ? "fotóstúdió alaprajz" : studioImages[0].alt} width={studioImages[0].width} height={studioImages[0].height} loading="lazy" decoding="async" /></div>
      <div class="studio-overview__facts">
        <h2 id="studio-facts-title">{studio.heading}</h2>
        <ul>{studio.facts.map((fact) => <li>{fact}</li>)}</ul>
      </div>
    </section>
    <section class="studio-gallery" aria-label={hu ? "Képgaléria a stúdióról" : "Photo gallery of the studio"}>
      <div class="studio-gallery__track">
        <div class="studio-gallery__column studio-gallery__column--wide">
          {[0, 1, 2].map((index) => <button class={`studio-gallery__item studio-gallery__item--${studioGalleryImages[index].layout}`} type="button" data-gallery-image data-gallery-index={index} data-gallery-src={studioGalleryImages[index].src} data-gallery-alt={studioGalleryImages[index].alt} aria-label={hu ? `Kép megnyitása: ${index + 1}` : `Open image ${index + 1}`}><img src={studioGalleryImages[index].src} alt={studioGalleryImages[index].alt} width={studioGalleryImages[index].width} height={studioGalleryImages[index].height} loading="eager" decoding="async" /></button>)}
        </div>
        <div class="studio-gallery__column studio-gallery__column--medium">
          {[3, 4].map((index) => <button class={`studio-gallery__item studio-gallery__item--${studioGalleryImages[index].layout}`} type="button" data-gallery-image data-gallery-index={index} data-gallery-src={studioGalleryImages[index].src} data-gallery-alt={studioGalleryImages[index].alt} aria-label={hu ? `Kép megnyitása: ${index + 1}` : `Open image ${index + 1}`}><img src={studioGalleryImages[index].src} alt={studioGalleryImages[index].alt} width={studioGalleryImages[index].width} height={studioGalleryImages[index].height} loading="eager" decoding="async" /></button>)}
        </div>
        <div class="studio-gallery__column studio-gallery__column--narrow">
          {[5, 6].map((index) => <button class={`studio-gallery__item studio-gallery__item--${studioGalleryImages[index].layout}`} type="button" data-gallery-image data-gallery-index={index} data-gallery-src={studioGalleryImages[index].src} data-gallery-alt={studioGalleryImages[index].alt} aria-label={hu ? `Kép megnyitása: ${index + 1}` : `Open image ${index + 1}`}><img src={studioGalleryImages[index].src} alt={studioGalleryImages[index].alt} width={studioGalleryImages[index].width} height={studioGalleryImages[index].height} loading="eager" decoding="async" /></button>)}
        </div>
        {studioGalleryImages.slice(7, 19).map((image, offset) => { const index = offset + 7; return <div class="studio-gallery__column studio-gallery__column--portrait"><button class="studio-gallery__item studio-gallery__item--portrait" type="button" data-gallery-image data-gallery-index={index} data-gallery-src={image.src} data-gallery-alt={image.alt} aria-label={hu ? `Kép megnyitása: ${index + 1}` : `Open image ${index + 1}`}><img src={image.src} alt={image.alt} width={image.width} height={image.height} loading="eager" decoding="async" /></button></div>; })}
        <div class="studio-gallery__column studio-gallery__column--final">
          {[19, 20, 21].map((index) => <button class={`studio-gallery__item studio-gallery__item--${studioGalleryImages[index].layout}`} type="button" data-gallery-image data-gallery-index={index} data-gallery-src={studioGalleryImages[index].src} data-gallery-alt={studioGalleryImages[index].alt} aria-label={hu ? `Kép megnyitása: ${index + 1}` : `Open image ${index + 1}`}><img src={studioGalleryImages[index].src} alt={studioGalleryImages[index].alt} width={studioGalleryImages[index].width} height={studioGalleryImages[index].height} loading="eager" decoding="async" /></button>)}
        </div>
        <div class="studio-gallery__column studio-gallery__column--extra"><button class="studio-gallery__item studio-gallery__item--extra-wide" type="button" data-gallery-image data-gallery-index="22" data-gallery-src={studioGalleryImages[22].src} data-gallery-alt={studioGalleryImages[22].alt} aria-label={hu ? "Kép megnyitása: 23" : "Open image 23"}><img src={studioGalleryImages[22].src} alt={studioGalleryImages[22].alt} width={studioGalleryImages[22].width} height={studioGalleryImages[22].height} loading="eager" decoding="async" /></button></div>
      </div>
    </section>
    <section class="studio-backdrops" aria-labelledby="backdrops-title">
      <div class="studio-backdrops__intro">
        <h2 id="backdrops-title">{studio.backdropHeading}</h2>
        <span class="studio-backdrops__rule" aria-hidden="true"></span>
        <p>{studio.backdropCopy}</p>
        <p><strong>{studio.backdropCta}</strong></p>
      </div>
      <div class="studio-backdrops__grid">
        {studioBackdropImages.map((backdrop, index) => <figure><img src={backdrop.src} alt={hu ? `${studio.backdropList[index]} háttér` : backdrop.alt} width="152" height="152" loading="lazy" decoding="async" /><figcaption>{studio.backdropList[index]}</figcaption></figure>)}
      </div>
      <p class="studio-backdrops__note">{studio.backdropNote}</p>
    </section>
    <BookingFooter page={page} />
    <dialog class="studio-lightbox studio-lightbox--page" aria-label={hu ? "Stúdió képgaléria" : "Studio photo gallery"} data-studio-lightbox>
      <button class="studio-lightbox__close" type="button" aria-label={hu ? "Galéria bezárása" : "Close gallery"} data-lightbox-close>×</button>
      <button class="studio-lightbox__previous" type="button" aria-label={hu ? "Előző kép" : "Previous image"} data-lightbox-previous>←</button>
      <img data-lightbox-image src={studioGalleryImages[0].src} alt={studioGalleryImages[0].alt} />
      <button class="studio-lightbox__next" type="button" aria-label={hu ? "Következő kép" : "Next image"} data-lightbox-next>→</button>
    </dialog>
  </>
) : page.path === "/selfie-studio-budapest" || page.path === "/hu/selfie-studio-budapest" ? (
  <>
    <section class="selfie-hero" aria-labelledby="selfie-title">
      <img class="selfie-hero__image" src={selfieImages.hero.src} alt={selfieImages.hero.alt} width={selfieImages.hero.width} height={selfieImages.hero.height} fetchpriority="high" decoding="async" />
      <div class="selfie-hero__veil" aria-hidden="true"></div>
      <div class="selfie-hero__content">
        <h1 id="selfie-title">{selfie.heroHeading.split("\n").map((line) => <span>{line}</span>)}</h1>
        <h2>{selfie.heroSubheading}</h2>
        <p>{selfie.heroCopy}</p>
        <a class="button button--dark" data-gtag-event="selfie_booking_click" data-gtag-location="selfie-studio-hero" href={site.selfieBookingUrl} target="_blank" rel="noreferrer noopener">{selfie.heroCta}</a>
      </div>
    </section>
    <section class="selfie-process" aria-labelledby="selfie-process-title">
      <h2 id="selfie-process-title">{selfie.processHeading}</h2>
      <ol class="selfie-process__list">
        {selfie.processSteps.map((step) => { const note = step.length === 3 ? step[1] : ""; return <li><span>{step[0]}</span>{note && <small>{note}</small>}</li>; })}
      </ol>
      <div class="selfie-process__images">
        {selfieImages.process.map((image) => <img src={image.src} alt={image.alt} width={image.width} height={image.height} loading="lazy" decoding="async" />)}
      </div>
    </section>
    <section class="selfie-gallery" aria-labelledby="selfie-gallery-title">
      <h2 id="selfie-gallery-title">{selfie.galleryHeading}</h2>
      <div class="selfie-gallery__track">{selfieImages.gallery.map((image) => <figure><img src={image.src} alt={image.alt} width={image.width} height={image.height} loading="lazy" decoding="async" /></figure>)}</div>
    </section>
    <section class="selfie-customize" aria-labelledby="selfie-customize-title">
      <div class="selfie-customize__intro">
        <h2 id="selfie-customize-title">{selfie.customizeHeading}</h2>
        <p>{selfie.customizeCopy}</p>
      </div>
      <div class="selfie-controls">
        {selfie.controls.map((control) => { const label = control[0] as string; const options = control[1] as string[]; const ids = control[2] as string[]; return <article class="selfie-control"><h3>{label}</h3><div class="selfie-control__images">{ids.map((id, index) => { const image = selfieImages.controls.get(id); return image && <figure><img src={image.src} alt={image.alt} width={image.width} height={image.height} loading="lazy" decoding="async" /><figcaption>{options[index]}</figcaption></figure>; })}</div><p class="selfie-control__options">{options.join(", ")}</p></article>; })}
      </div>
      <p class="selfie-customize__custom">{selfie.customizeCustom}</p>
    </section>
    <section class="selfie-benefits" aria-labelledby="selfie-benefits-title">
      <div class="selfie-benefits__intro"><h2 id="selfie-benefits-title">{selfie.benefitsHeading}</h2><p>{selfie.benefitsIntro}</p></div>
      <div class="selfie-benefits__grid">{selfie.benefits.map(([title, copy], index) => { const image = selfieImages.benefits[index]; return <article class="selfie-benefit"><h3>{title}</h3><img src={image.src} alt={image.alt} width={image.width} height={image.height} loading="lazy" decoding="async" /><p>{copy}</p></article>; })}</div>
    </section>
    <section class="selfie-prices" aria-labelledby="selfie-prices-title">
      <div class="selfie-prices__intro"><h2 id="selfie-prices-title">{selfie.pricesHeading}</h2></div>
      <div class="selfie-prices__grid">{selfie.prices.map((priceItem, index) => { const title = priceItem[0] as string; const details = priceItem[1] as string[]; const price = priceItem[2] as string; const image = selfieImages.prices[index]; return <article class="selfie-price"><img src={image.src} alt={image.alt} width={image.width} height={image.height} loading="lazy" decoding="async" /><div class="selfie-price__copy"><h3>{title}</h3>{details.map((detail) => <p>{detail}</p>)}<strong>{price}</strong></div></article>; })}</div>
      <div class="selfie-extras"><h3>{selfie.extrasHeading}</h3>{selfie.extras.map(([title, price, note]) => <p><strong>{title}</strong><span>{price}</span>{note && <small>{note}</small>}</p>)}</div>
      <div class="selfie-reserve"><h3>{selfie.reserveHeading}</h3><p>{selfie.reserveCopy}</p><a class="button button--dark" data-gtag-event="selfie_booking_click" data-gtag-location="selfie-studio-reserve" href={site.selfieBookingUrl} target="_blank" rel="noreferrer noopener">{selfie.reserveCta}</a></div>
    </section>
    <section class="selfie-location" aria-labelledby="selfie-location-title">
      <div><h2 id="selfie-location-title">{selfie.szegedHeading}</h2><p>{selfie.szegedCopy}</p><a class="text-link" href={hu ? "https://www.aetherstudio-szeged.com/" : "https://www.aetherstudio-szeged.com/en"} target="_blank" rel="noreferrer noopener">{selfie.szegedCta}</a></div>
    </section>
  </>
) : collectionPage ? (
  <>
    <section class="collection-hero" aria-labelledby="collection-title">
      <h1 id="collection-title">{collectionPage.heroHeading}</h1>
      <p>{collectionPage.heroCopy}</p>
    </section>
    {collectionSections.map((section, index) => (
      <section class={`collection-section collection-section--${section.galleries[0].imageKey}`} aria-labelledby={`collection-section-${index}`}>
        <div class="collection-section__intro">
          <h2 id={`collection-section-${index}`}>{section.heading}</h2>
          {section.subheading && <h3>{section.subheading}</h3>}
          <span class="collection-section__rule" aria-hidden="true"></span>
          {section.paragraphs.map((paragraph) => <p>{paragraph}</p>)}
        </div>
        {section.galleries.map((gallery) => <div class="collection-gallery-wrap">
          <div class={`collection-gallery collection-gallery--${gallery.imageKey}`}>
            {collectionImageMap[gallery.imageKey].map((image) => <figure><button class="collection-gallery__item" type="button" data-gallery-image data-gallery-src={image.src} data-gallery-alt={image.alt} aria-label={hu ? "Kép megnyitása" : "Open image"}><img src={image.src} alt={image.alt} width={image.width} height={image.height} loading="lazy" decoding="async" /></button></figure>)}
          </div>
          {gallery.note && <div class="collection-gallery__note"><p>{gallery.note[0]}</p>{gallery.note[1] && <strong>{gallery.note[1]}</strong>}</div>}
        </div>)}
      </section>
    ))}
    <dialog class="studio-lightbox studio-lightbox--page" aria-label={hu ? "Kellék- és felszerelés galéria" : "Props and equipment gallery"} data-studio-lightbox>
      <button class="studio-lightbox__close" type="button" aria-label={hu ? "Galéria bezárása" : "Close gallery"} data-lightbox-close>×</button>
      <button class="studio-lightbox__previous" type="button" aria-label={hu ? "Előző kép" : "Previous image"} data-lightbox-previous>←</button>
      <img data-lightbox-image src={collectionImageMap[collectionSections[0].galleries[0].imageKey][0].src} alt="" />
      <button class="studio-lightbox__next" type="button" aria-label={hu ? "Következő kép" : "Next image"} data-lightbox-next>→</button>
    </dialog>
  </>
) : packagesPage ? (
  <>
    <section class="packages-hero" aria-labelledby="packages-title">
      <h1 id="packages-title">{packagesPageAny.heroHeading}</h1>
      <p>{packagesPageAny.heroCopy}</p>
    </section>
    <section class="packages-intro" aria-labelledby="packages-intro-title">
      <div>
        <h2 id="packages-intro-title">{packagesPageAny.heroHeading}</h2>
        {packagesPageAny.intro.map((paragraph: string) => <p>{paragraph}</p>)}
      </div>
    </section>
    <section class="packages-primary" aria-label={hu ? "Fotózási csomagok" : "Photoshoot packages"}>
      <div class="packages-card-grid">
        {packagesPageAny.packages.map((packageItem: any) => <article class="packages-card packages-card--primary">
          <h2>{packageItem.title}</h2>
          <p>{packageItem.description}</p>
          <strong>{packageItem.price}</strong>
          <ul>{packageItem.features.map((feature: string) => <li>{feature}</li>)}</ul>
        </article>)}
      </div>
    </section>
    <section class="packages-upgrades" aria-labelledby="packages-upgrades-title">
      <div class="packages-upgrades__intro">
        <h2 id="packages-upgrades-title">{packagesPageAny.upgradesHeading}</h2>
        <p>{packagesPageAny.upgradesIntro}</p>
      </div>
      <div class="packages-card-grid">
        {packagesPageAny.upgrades.map((upgrade: any) => <article class="packages-card packages-card--upgrade">
          <h3>{upgrade.title}</h3>
          <p>{upgrade.description}</p>
          <span>{upgrade.priceLabel}</span>
          <strong>{upgrade.price}</strong>
          <a class="button button--outline" href={upgrade.href}>{upgrade.cta}</a>
        </article>)}
      </div>
    </section>
    <section class="packages-contact" style={`background-image: linear-gradient(rgba(0,0,0,.25), rgba(0,0,0,.25)), url(${packagesImages.contact.src});`} aria-labelledby="packages-contact-title">
      <div class="packages-contact__card">
        <h2 id="packages-contact-title">{packagesPageAny.contactHeading}</h2>
        <a class="text-link" href={`mailto:${site.email}?subject=${packagesPageAny.contactSubject}`}>{site.email}</a>
      </div>
    </section>
  </>
) : modelPage ? (
  <>
    <section class="models-hero" aria-labelledby="models-title">
      <h1 id="models-title">{modelPageAny.heroHeading}</h1>
      <p>{modelPageAny.heroCopy}</p>
    </section>
    <section class="models-intro" aria-labelledby="models-intro-title">
      <div class="models-intro__copy">
        <h2 id="models-intro-title">{modelPageAny.heroHeading}</h2>
        {modelPageAny.intro.map((paragraph: string) => <p>{paragraph}</p>)}
      </div>
      <div class="models-fee">
        <h2>{modelPageAny.feeTitle}</h2>
        <strong>{modelPageAny.feePrice}</strong>
        <a class="button button--outline" href="#models-contact">{modelPageAny.contactLabel}</a>
      </div>
    </section>
    <section class="models-list" aria-labelledby="models-list-title">
      <h2 id="models-list-title">{modelPageAny.feeTitle}</h2>
      {modelPageAny.people.map((profile: any) => {
        const gallery = modelPeopleImages[profile.imageIndex];
        const firstImage = gallery[0];
        return <article class="model-card" data-model-card>
          <div class="model-gallery">
            <div class="model-gallery__thumbs" aria-label={`${profile.name} ${hu ? "képei" : "images"}`}>
              {gallery.map((image: any, index: number) => <button class={`model-gallery__thumb${index === 0 ? " is-active" : ""}`} type="button" data-model-thumb data-model-src={image.src} data-model-alt={image.alt} data-model-position={image.objectPosition} aria-label={`${profile.name}, ${hu ? "kép" : "image"} ${index + 1}`} aria-pressed={index === 0 ? "true" : "false"}><img src={image.src} alt="" width="100" height="100" style={`object-position: ${image.objectPosition};`} loading="lazy" decoding="async" /></button>)}
            </div>
            <button class="model-gallery__main" type="button" data-gallery-image data-model-main data-gallery-src={firstImage.src} data-gallery-alt={firstImage.alt} aria-label={hu ? `Kép megnyitása: ${profile.name}` : `Open ${profile.name} image`}>
              <img data-model-main-image src={firstImage.src} alt={firstImage.alt} width={firstImage.width} height={firstImage.height} style={`object-position: ${firstImage.objectPosition};`} loading="lazy" decoding="async" />
            </button>
          </div>
          <div class="model-card__body">
            <h3>{profile.name}</h3>
            <h4>{modelPageAny.measurementsTitle}</h4>
            <div class="model-measurements">{profile.measurements.map((group: string[]) => <p>{group.map((line) => <span>{line}</span>)}</p>)}</div>
            <div class="model-card__action"><p>{profile.contact}</p><a class="button button--outline" href="#models-contact">{modelPageAny.contactLabel}</a></div>
          </div>
        </article>;
      })}
    </section>
    <section class="models-animal" aria-labelledby="models-animal-title">
      <div class="models-animal__intro">
        <h2 id="models-animal-title">{modelPageAny.animalHeading}</h2>
        {modelPageAny.animalIntro.map((paragraph: string) => <p>{paragraph}</p>)}
      </div>
      <article class="model-animal">
        <img src={modelImages.animal.src} alt={modelImages.animal.alt} width={modelImages.animal.width} height={modelImages.animal.height} loading="lazy" decoding="async" />
        <div class="model-animal__body">
          <h3>{modelPageAny.animal.name}</h3>
          <p>{modelPageAny.animal.description}</p>
          <strong>{modelPageAny.animal.priceLabel}</strong>
          <b>{modelPageAny.animal.price}</b>
          <h4>{modelPageAny.animal.measurementsTitle}</h4>
          <div class="model-measurements">{modelPageAny.animal.measurements.map((group: string[]) => <p>{group.map((line) => <span>{line}</span>)}</p>)}</div>
          <div class="model-card__action"><p>{modelPageAny.animal.contactPrefix}{modelPageAny.animal.contactSuffix}</p><a class="button button--outline" href="#models-contact">{modelPageAny.contactLabel}</a></div>
        </div>
      </article>
    </section>
    <section class="models-platform" aria-labelledby="models-platform-title">
      <div class="models-platform__copy">
        <h2 id="models-platform-title">{modelPageAny.platform.heading}</h2>
        <h3>{modelPageAny.platform.subheading}</h3>
        {modelPageAny.platform.paragraphs.map((paragraph: string) => <p>{paragraph}</p>)}
        <a class="button button--outline" href="https://naebula.app/" target="_blank" rel="noreferrer noopener">{modelPageAny.platform.cta}</a>
      </div>
      <img src={modelImages.platform.src} alt={modelImages.platform.alt} width={modelImages.platform.width} height={modelImages.platform.height} loading="lazy" decoding="async" />
    </section>
    <dialog class="studio-lightbox studio-lightbox--page" aria-label={hu ? "Modellgaléria" : "Model gallery"} data-studio-lightbox>
      <button class="studio-lightbox__close" type="button" aria-label={hu ? "Galéria bezárása" : "Close gallery"} data-lightbox-close>×</button>
      <button class="studio-lightbox__previous" type="button" aria-label={hu ? "Előző kép" : "Previous image"} data-lightbox-previous>←</button>
      <img data-lightbox-image src={modelPeopleImages[0][0].src} alt={modelPeopleImages[0][0].alt} />
      <button class="studio-lightbox__next" type="button" aria-label={hu ? "Következő kép" : "Next image"} data-lightbox-next>→</button>
    </dialog>
  </>
) : artistPage ? (
  <>
    <section class="artist-hero" aria-labelledby="artist-title">
      <h1 id="artist-title">{artistPageAny.heroHeading}</h1>
      <p>{artistPageAny.heroCopy}</p>
    </section>
    <section class="artist-intro" aria-labelledby="artist-intro-title">
      <div class="artist-intro__copy">
        <h2 id="artist-intro-title">{hu ? "rezidens alkotók" : "resident artists"}</h2>
        {artistPageAny.intro.map((paragraph: string) => <p>{paragraph}</p>)}
      </div>
      {artistPageAny.packages?.length > 0 && <div class="artist-packages">
        {artistPageAny.packages.map((packageItem: any) => <article class="artist-package">
          <h3>{packageItem.title}</h3>
          <p>{packageItem.description}</p>
          <strong>{packageItem.price}</strong>
          <ul>{packageItem.features.map((feature: string) => <li>{feature}</li>)}</ul>
        </article>)}
      </div>}
    </section>
    <section class={`artist-profiles artist-profiles--${artistType}`} aria-label={hu ? "Rezidens alkotók" : "Resident artists"}>
      <div class="artist-profiles__grid">
        {artistProfiles.map((profile: any) => <article class="artist-profile">
          <button class="artist-profile__image" type="button" data-gallery-image data-gallery-src={profile.image.src} data-gallery-alt={profile.image.alt} aria-label={hu ? "Kép megnyitása" : "Open image"}><img src={profile.image.src} alt={profile.image.alt} width={profile.image.width} height={profile.image.height} loading="lazy" decoding="async" /></button>
          <div class="artist-profile__body">
            <h3>{profile.name}</h3>
            <p class="artist-profile__role">{profile.role}</p>
            <p>{profile.specialties}</p>
            {profile.description && <p>{profile.description}</p>}
            {profile.price && <strong>{profile.price}</strong>}
            {profile.websiteUrl && <a class="text-link" href={profile.websiteUrl} target="_blank" rel="noreferrer noopener">{profile.website}</a>}
            {profile.more && <span class="artist-profile__more">{profile.more}</span>}
          </div>
          {profile.gallery?.length > 0 && <div class="artist-profile__gallery">{profile.gallery.map((image: any) => <button class="artist-profile__gallery-item" type="button" data-gallery-image data-gallery-src={image.src} data-gallery-alt={image.alt} aria-label={hu ? "Kép megnyitása" : "Open image"}><img src={image.src} alt={image.alt} width={image.width} height={image.height} loading="lazy" decoding="async" /></button>)}</div>}
        </article>)}
      </div>
</section>
    {artistPageAny.footerNote && <p class="artist-note">{artistPageAny.footerNote}</p>}
    <section class="artist-contact" aria-labelledby="artist-contact-title">
      <h2 id="artist-contact-title">{artistPageAny.contactHeading}</h2>
      <a class="text-link" href={`mailto:${site.email}?subject=${encodeURIComponent(hu ? "fotós foglalás" : "artist booking")}`}>{site.email}</a>
    </section>
    <dialog class="studio-lightbox studio-lightbox--page" aria-label={hu ? "Rezidens alkotók galériája" : "Resident artists gallery"} data-studio-lightbox>
      <button class="studio-lightbox__close" type="button" aria-label={hu ? "Galéria bezárása" : "Close gallery"} data-lightbox-close>×</button>
      <button class="studio-lightbox__previous" type="button" aria-label={hu ? "Előző kép" : "Previous image"} data-lightbox-previous>←</button>
      <img data-lightbox-image src={artistProfiles[0].image.src} alt={artistProfiles[0].image.alt} />
      <button class="studio-lightbox__next" type="button" aria-label={hu ? "Következő kép" : "Next image"} data-lightbox-next>→</button>
    </dialog>
  </>
) : isMentoring ? (
  <>
    <section class="service-hero" aria-labelledby="mentoring-title"><h1 id="mentoring-title">{page.h1}</h1><p>{mentoringPage.hero}</p></section>
    <section class="service-copy service-copy--intro"><p>{mentoringPage.intro}</p></section>
    <section class="service-section service-mentoring"><h2>{mentoringPage.priceHeading}</h2><div class="service-mentor-grid">{mentoringPage.packages.map((packageItem: any) => <article class="service-package"><h3>{packageItem.title}</h3><p>{packageItem.description}</p><strong>{packageItem.price}</strong><h4>{hu ? "A csomag tartalmaz mindent, amire a kurzus során szükséged lehet:" : "The package includes everything you need to complete the course:"}</h4><ul>{packageItem.includes.map((item: string) => <li>{item}</li>)}</ul>{packageItem.lessons.map((lesson: any) => <div class="service-lesson"><h4>{lesson[0]}</h4><ul>{lesson[1].map((item: string) => <li>{item}</li>)}</ul></div>)}<p class="service-mentor-free">{packageItem.free}</p><p>{hu ? "Kapsz 2 ingyen órát a studióban, hogy önállóan fotózhass. A szomszéd szobában leszünk, ha mégis segítségre van szükséged." : "You can do your first shoot alone - we will be in the next room if you need help."}</p></article>)}</div></section>
    <section class="service-contact"><h2>{mentoringPage.signHeading}</h2><p>{mentoringPage.signBody}</p><a class="text-link" href={`mailto:${site.email}?subject=${encodeURIComponent(hu ? "mentor program" : "mentoring in aether art space")}`}>{site.email}</a></section>
  </>
) : isIdPhoto ? (
  <>
    <section class="id-photo-hero" aria-labelledby="id-photo-title"><div><h1 id="id-photo-title">{page.h1}</h1><h2>{hu ? "egy órán belül Budapesten" : "within an hour in Budapest"}</h2><p>{hu ? "Ha dokumentumaidhoz szabványos fényképre van szükséged digitális vagy nyomtatott formában, fix áron elkészíttetheted stúdiónkban, és egy órán belül megkapod a végleges képeket." : "If you need a standard photograph in either digital or printed form for your documents, you can have it made in our studio for a fixed price and receive the final images within an hour in the center of Budapest."}</p><p>{hu ? "A fotók standard 3x5cm-es igazolványképek fehér háttérrel, amik megfelelnek útlevélhez, igazolványokhoz, vízumhoz és minden egyéb dokumentumhoz." : "The photos are standard 3x5cm with white background suitable and universally accepted to passports, visas, IDs and any other kind of document."}</p></div><img src={specialImages.id[0].src} alt={specialImages.id[0].alt} width="1000" height="1002" fetchpriority="high" decoding="async" /></section>
    <section class="id-photo-prices" aria-label={hu ? "Igazolványkép árak" : "ID photo prices"}><article><h2>{hu ? "digitális igazolványkép / útlevélkép" : "digital ID / passport photo"}</h2><strong>5.000. HUF</strong></article><article><h2>{hu ? "digitális + nyomtatott igazolványkép / útlevélkép (4 db)" : "digital + printed ID / passport photo (x4)"}</h2><strong>7.000. HUF</strong></article></section>
    <section class="id-photo-booking"><a class="button" data-gtag-event="booking_click" data-gtag-location="id-photo" href="https://cal.com/aether-studio/id-shoot?overlayCalendar=true">{hu ? "foglalj időpontot" : "Book an ID shoot now"}</a><p><strong>{hu ? "Az igazolványképek készítéséhez előzetes foglalás szükséges!" : "Scheduled appointments required for the ID shoots!"}</strong></p><p>{hu ? "A nyomtatott igazolványképek 15 perc alatt készülnek el. Visszatérhetsz érte, amikor neked alkalmas, vagy megvárhatod studiónkban, míg elkészülnek." : "Printed ID photos take about 15min - you can come back to grab them whenever you wait in our studio for your pictures."}</p></section>
    <section class="service-gallery service-gallery--id" aria-label={hu ? "Fotóstúdió galéria" : "Photo studio gallery"}>{specialImages.id.slice(1).map((imageData: any, index: number) => <button type="button" data-gallery-image data-gallery-index={index} data-gallery-src={imageData.src} data-gallery-alt={imageData.alt} aria-label={hu ? `Kép megnyitása: ${index + 1}` : `Open image ${index + 1}`}><img src={imageData.src} alt={imageData.alt} width={imageData.width} height={imageData.height} loading="lazy" decoding="async" /></button>)}</section>
    <dialog class="studio-lightbox studio-lightbox--page" aria-label={hu ? "Fotóstúdió galéria" : "Photo studio gallery"} data-studio-lightbox><button class="studio-lightbox__close" type="button" aria-label={hu ? "Galéria bezárása" : "Close gallery"} data-lightbox-close>×</button><button class="studio-lightbox__previous" type="button" aria-label={hu ? "Előző kép" : "Previous image"} data-lightbox-previous>←</button><img data-lightbox-image src={specialImages.id[1].src} alt={specialImages.id[1].alt} /><button class="studio-lightbox__next" type="button" aria-label={hu ? "Következő kép" : "Next image"} data-lightbox-next>→</button></dialog>
  </>
) : specialPage ? (
  <>
    <section class="service-hero" aria-labelledby="special-title"><h1 id="special-title">{page.h1}</h1><p>{specialPageAny.hero}</p></section>
    <section class="service-copy service-copy--intro">{specialPageAny.intro.map((paragraph: string) => <p>{paragraph}</p>)}</section>
    {specialImagesAny?.length > 0 && <section class="service-gallery service-gallery--special" aria-label={hu ? "Fotózási galéria" : "Photography gallery"}>{specialImagesAny.map((imageData: any, index: number) => <button type="button" data-gallery-image data-gallery-index={index} data-gallery-src={imageData.src} data-gallery-alt={imageData.alt} aria-label={hu ? `Kép megnyitása: ${index + 1}` : `Open image ${index + 1}`}><img src={imageData.src} alt={imageData.alt} width={imageData.width} height={imageData.height} loading={index < 3 ? "eager" : "lazy"} decoding="async" /></button>)}</section>}
    {specialType === "models" && <section class="service-section service-special-sections">{specialPageAny.sections.map((item: string[]) => <div class="service-info"><h3>{item[0]}</h3><p>{item[1]}</p></div>)}<div class="service-model-card"><div><h3>{specialPageAny.packageHeading}</h3><p>{specialPageAny.packageLead}</p><a class="text-link" href={`mailto:${site.email}?subject=${encodeURIComponent(hu ? "modell fotózás" : "model photoshoot")}`}>{specialPageAny.offer}</a></div><img src={specialImages.modelCard.src} alt={specialImages.modelCard.alt} width={specialImages.modelCard.width} height={specialImages.modelCard.height} loading="lazy" decoding="async" /></div></section>}
    {specialType === "glamour" && <section class="service-section service-special-sections"><p class="service-note">{specialPageAny.firstTimers}</p>{specialPageAny.sections.map((item: string[]) => <div class="service-info"><h3>{item[0]}</h3><p>{item[1]}</p></div>)}</section>}
    {specialType !== "pet" && <section class="service-section service-section--packages"><h2>{specialPageAny.packageHeading}</h2><p>{specialPageAny.packageLead}</p><div class="service-package-grid">{commonPhotoPackages[hu ? "hu" : "en"].map((packageItem: any) => <article class="service-package"><h3>{packageItem.title}</h3><p>{packageItem.description}</p><strong>{packageItem.price}</strong><ul>{packageItem.features.map((feature: string) => <li>{feature}</li>)}</ul></article>)}</div></section>}
    {specialType !== "pet" && <section class="service-upgrades"><h3>{hu ? "kiegészítő szolgáltatások" : "upgrade your shoot"}</h3><p>{hu ? "vedd igénybe teljes kreatív csapatunkat" : "use our entire creative team"}</p><p>{hu ? "foglalj le mindent egyszerre fotózásodhoz" : "save the hassle and book everything in one go"}</p><div class="service-upgrade-grid">{commonUpgrades[hu ? "hu" : "en"].map((item: any) => <article><h4>{item.title}</h4><span>{item.priceLabel}</span><strong>{item.price}</strong></article>)}</div></section>}
    <section class="service-section service-section--studio"><h2>{specialPageAny.studioHeading}</h2><p>{specialPageAny.studioIntro}</p><ul>{specialPageAny.studioBullets.map((bullet: string) => <li>{bullet}</li>)}</ul><p>{specialPageAny.studioTail}</p></section>
    {specialPageAny.faq && <section class="service-faq"><h2>{specialPageAny.faqHeading}</h2>{specialPageAny.faq.map((item: string[]) => <details><summary>{item[0]}</summary><p>{item[1]}</p></details>)}</section>}
    {specialImagesAny?.length > 0 && <dialog class="studio-lightbox studio-lightbox--page" aria-label={hu ? "Fotózási galéria" : "Photography gallery"} data-studio-lightbox><button class="studio-lightbox__close" type="button" aria-label={hu ? "Galéria bezárása" : "Close gallery"} data-lightbox-close>×</button><button class="studio-lightbox__previous" type="button" aria-label={hu ? "Előző kép" : "Previous image"} data-lightbox-previous>←</button><img data-lightbox-image src={specialImagesAny[0].src} alt={specialImagesAny[0].alt} /><button class="studio-lightbox__next" type="button" aria-label={hu ? "Következő kép" : "Next image"} data-lightbox-next>→</button></dialog>}
  </>
) : servicePage ? (
  <>
    <section class="service-hero" aria-labelledby="service-title">
      <h1 id="service-title">{page.h1}</h1>
      <p>{servicePageAny.hero}</p>
    </section>
    <section class="service-copy service-copy--intro">
      {servicePageAny.intro.map((paragraph: string) => <p>{paragraph}</p>)}
    </section>
    {serviceType === "commercial" && <>
      <section class="service-gallery service-gallery--commercial" aria-label={hu ? "Reklámfotózási galéria" : "Commercial photography gallery"}>
        {serviceImagesAny.map((imageData: any, index: number) => <button type="button" data-gallery-image data-gallery-index={index} data-gallery-src={imageData.src} data-gallery-alt={imageData.alt} aria-label={hu ? `Kép megnyitása: ${index + 1}` : `Open image ${index + 1}`}><img src={imageData.src} alt={imageData.alt} width={imageData.width} height={imageData.height} loading={index < 4 ? "eager" : "lazy"} decoding="async" /></button>)}
      </section>
      <section class="service-section service-section--commercial" aria-labelledby="commercial-services-title">
        <h2 id="commercial-services-title">{servicePageAny.servicesHeading}</h2><p>{servicePageAny.servicesLead}</p>
        <div class="service-card-grid">{servicePageAny.services.map((item: string[]) => <article><h3>{item[0]}</h3><p>{item[1]}</p></article>)}</div>
        <p>{servicePageAny.servicesTail}</p><a class="text-link" href={`mailto:${site.email}?subject=${encodeURIComponent(hu ? "reklámfotózás ajánlat" : "quote for commercial photography")}`}>{servicePageAny.offer}</a>
      </section>
      <section class="service-clients" aria-labelledby="service-clients-title"><h2 id="service-clients-title">{servicePageAny.clients}</h2><div>{serviceImages.clients.map((imageData: any) => <img src={imageData.src} alt={imageData.alt} width="150" height="150" loading="lazy" decoding="async" />)}</div></section>
    </>}
    {serviceType === "portrait" && <>
      <section class="service-gallery service-gallery--portrait" aria-label={hu ? "Portréfotózási galéria" : "Portrait photography gallery"}>{serviceImagesAny.map((imageData: any, index: number) => <button type="button" data-gallery-image data-gallery-index={index} data-gallery-src={imageData.src} data-gallery-alt={imageData.alt} aria-label={hu ? `Kép megnyitása: ${index + 1}` : `Open image ${index + 1}`}><img src={imageData.src} alt={imageData.alt} width={imageData.width} height={imageData.height} loading={index < 3 ? "eager" : "lazy"} decoding="async" /></button>)}</section>
      <section class="service-section service-section--packages"><p class="service-note">{servicePageAny.firstTimers}</p><h2>{servicePageAny.packageHeading}</h2><p>{servicePageAny.packageLead}</p><div class="service-package-grid">{servicePackages.map((packageItem: any) => <article class="service-package"><h3>{packageItem.title}</h3><p>{packageItem.description}</p><strong>{packageItem.price}</strong><ul>{packageItem.features.map((feature: string) => <li>{feature}</li>)}</ul></article>)}</div></section>
      <section class="service-upgrades"><h3>{hu ? "kiegészítő szolgáltatások" : "upgrade your shoot"}</h3><p>{hu ? "vedd igénybe teljes kreatív csapatunkat" : "use our entire creative team"}</p><p>{hu ? "foglalj le mindent egyszerre fotózásodhoz" : "save the hassle and book everything in one go"}</p><div class="service-upgrade-grid">{serviceUpgrades.map((item: any) => <article><h4>{item.title}</h4><span>{item.priceLabel}</span><strong>{item.price}</strong><a class="text-link" href={item.href}>{item.href.startsWith("mailto:") ? (hu ? "kérj ajánlatot" : "ask for an offer") : (hu ? "bővebben" : "learn more")}</a></article>)}</div></section>
    </>}
    {serviceType === "fitness" && <>
      <section class="service-gallery service-gallery--fitness" aria-label={hu ? "Fitness és jóga fotózási galéria" : "Fitness and yoga photography gallery"}>{serviceImagesAny.map((imageData: any, index: number) => <button type="button" data-gallery-image data-gallery-index={index} data-gallery-src={imageData.src} data-gallery-alt={imageData.alt} aria-label={hu ? `Kép megnyitása: ${index + 1}` : `Open image ${index + 1}`}><img src={imageData.src} alt={imageData.alt} width={imageData.width} height={imageData.height} loading={index < 4 ? "eager" : "lazy"} decoding="async" /></button>)}</section>
      <section class="service-section service-section--audience"><h2>{servicePageAny.audienceHeading}</h2><p>{servicePageAny.audienceIntro}</p><div class="service-audience-grid">{servicePageAny.audience.map((item: string[], index: number) => <article><img src={fitnessAudienceImages[index].src} alt={fitnessAudienceImages[index].alt} width="258" height="258" loading="lazy" decoding="async" /><h3>{item[0]}</h3><p>{item[1]}</p></article>)}</div></section>
      <section class="service-section service-section--packages"><h2>{servicePageAny.packageHeading}</h2><p>{servicePageAny.packageLead}</p><div class="service-package-grid">{servicePageAny.packages.map((packageItem: any) => <article class="service-package"><h3>{packageItem.title}</h3><p>{packageItem.description}</p><strong>{packageItem.price}</strong><p class="service-package__includes">{hu ? "A csomag tartalma:" : "This package includes:"}</p><ul>{packageItem.features.map((feature: string) => <li>{feature}</li>)}</ul></article>)}</div></section>
      <section class="service-upgrades"><h3>{servicePageAny.upgradesHeading}</h3><p>{servicePageAny.upgradesSubheading}</p><p>{servicePageAny.upgradesIntro}</p><div class="service-upgrade-grid">{serviceUpgrades.map((item: any) => <article><h4>{item.title}</h4><span>{item.priceLabel}</span><strong>{item.price}</strong></article>)}</div></section>
    </>}
    {serviceType === "corporate" && <>
      <section class="service-section service-section--corporate-intro"><h2>{servicePageAny.whatHeading}</h2><p>{servicePageAny.whatIntro}</p><ul>{servicePageAny.services.map((item: string) => <li>{item}</li>)}</ul>{servicePageAny.sections.map((item: string[]) => <div class="service-info"><h3>{item[0]}</h3><p>{item[1]}</p></div>)}</section>
      <section class="service-gallery service-gallery--corporate" aria-label={hu ? "Vállalati fotózási galéria" : "Corporate photography gallery"}>{serviceImagesAny.map((imageData: any, index: number) => <button type="button" data-gallery-image data-gallery-index={index} data-gallery-src={imageData.src} data-gallery-alt={imageData.alt} aria-label={hu ? `Kép megnyitása: ${index + 1}` : `Open image ${index + 1}`}><img src={imageData.src} alt={imageData.alt} width={imageData.width} height={imageData.height} loading={index < 3 ? "eager" : "lazy"} decoding="async" /></button>)}</section>
      <section class="service-section service-section--packages"><h2>{servicePageAny.packageHeading}</h2><p>{servicePageAny.packageLead}</p><div class="service-package-grid service-package-grid--corporate">{servicePageAny.packages.map((packageItem: any) => <article class="service-package"><h3>{packageItem.title}</h3><p>{packageItem.description}</p><strong>{packageItem.price}</strong><p class="service-package__includes">{hu ? "A csomag tartalma:" : "This package includes:"}</p><ul>{packageItem.features.map((feature: string) => <li>{feature}</li>)}</ul></article>)}</div></section>
    </>}
    {serviceType !== "commercial" && <section class="service-section service-section--studio"><h2>{servicePageAny.studioHeading}</h2><p>{servicePageAny.studioIntro}</p><ul>{servicePageAny.studioBullets.map((bullet: string) => <li>{bullet}</li>)}</ul><p>{servicePageAny.studioTail}</p></section>}
    {serviceType === "corporate" && <><section class="service-section service-section--approach"><h2>{servicePageAny.approachHeading}</h2><p>{servicePageAny.approach}</p></section><section class="service-clients"><h2>{servicePageAny.clients}</h2><div>{serviceImages.clients.map((imageData: any) => <img src={imageData.src} alt={imageData.alt} width="150" height="150" loading="lazy" decoding="async" />)}</div></section><section class="service-faq"><h2>{servicePageAny.faqHeading}</h2>{servicePageAny.faq.map((item: string[]) => <details><summary>{item[0]}</summary><p>{item[1]}</p></details>)}</section></>}
    <dialog class="studio-lightbox studio-lightbox--page" aria-label={hu ? "Szolgáltatási galéria" : "Service gallery"} data-studio-lightbox><button class="studio-lightbox__close" type="button" aria-label={hu ? "Galéria bezárása" : "Close gallery"} data-lightbox-close>×</button><button class="studio-lightbox__previous" type="button" aria-label={hu ? "Előző kép" : "Previous image"} data-lightbox-previous>←</button><img data-lightbox-image src={serviceImagesAny[0].src} alt={serviceImagesAny[0].alt} /><button class="studio-lightbox__next" type="button" aria-label={hu ? "Következő kép" : "Next image"} data-lightbox-next>→</button></dialog>
  </>
) : page.path === "/post-booking" || page.path === "/hu/post-booking" ? (
  <>
    <section class="hero"><p class="eyebrow">{hu ? "foglalás visszaigazolása" : "booking confirmation"}</p><h1>{page.h1}</h1><p class="hero-lead">{hu ? "Hamarosan jelentkezünk a foglalásod részleteivel." : "We will be in touch shortly with the details of your booking."}</p></section>
    <section class="callout"><h2>{hu ? "Köszönjük, hogy az aether art space-t választottad." : "Thank you for choosing aether art space."}</h2><a class="button button--dark" href={hu ? "/hu" : "/"}>{hu ? "vissza a főoldalra" : "back to home"}</a></section>
  </>
) : page.section === "booking" ? (
  <>
    <section class="booking-intro"><h1>{page.h1}</h1><p>{hu ? "Az Aether Art Space-ben igyekszünk elérhető árakat kínálni, hogy te az igazán fontos dolgokra koncentrálhass – a fotózás mögötti kreativitásra." : "At aether art space, we offer fair and affordable pricing, so you can focus on what matters most – the creativity behind your shoot."}</p><p>{hu ? "Stúdiónk úgy van kialakítva, hogy támogassa a kreatív folyamatot, rugalmas csomagokat kínálva, amelyek különböző igényekhez igazodnak, legyen szó modellről, fotósról vagy vállalkozásról, aki lenyűgöző vizuális anyagot szeretne készíteni." : "Our studio is designed to elevate the creative process, with flexible packages that cater to different needs, whether you're a model, photographer, or business looking to capture stunning visuals."}</p><p>{hu ? "Ezen kívül számos kiegészítőt kínálunk helyben, kizárólag a budapesti stúdiónkban, mint például analóg fényképezőgép bérlés, ruhák, filmek és még sok más, hogy segítsünk életre kelteni a víziódat." : "We also offer a variety of extras on-site exclusive to our studio in Budapest, including analogue camera rentals, clothing, film, and more, to help bring your vision to life."}</p></section>
    <section class="booking-prices"><article><h2>{hu ? "Kedvezményes Stúdió idősávok" : "Discounted Studio Timeslots"}</h2><p>{hu ? "hétfőtől csütörtökig / 9:00-18:00" : "Monday to Thursday / 9:00 - 18:00"}</p><strong>{hu ? "7.000. Ft/óra" : "7.000. HUF / hour"}</strong></article><article><h2>{hu ? "Standard Stúdió idősávok" : "Standard Studio Timeslots"}</h2><p>{hu ? "hétfőtől csütörtökig 18:00-22:00 között; péntek és hétvége 10:00-22:00" : "Monday to Thursday 18:00-22:00; Friday & Weekend 9:00 - 22:00"}</p><strong>{hu ? "8.000. Ft/óra" : "8.000. HUF / hour"}</strong></article><article><h2>{hu ? "10 órás bérlet" : "10 hour Membership"}</h2><p>{hu ? "Bármelyik nap / óránként felbontható" : "Any day / can be broken up by the hour"}</p><strong>60.000. HUF</strong><span>{hu ? "akár 25% kedvezmény!" : "SAVE UP TO 37%"}</span></article></section>
    <p class="booking-note">{hu ? "Nyitvatartási időn kívül 10.000. HUF áron bérelhető a studió 0-24 - dobj egy emailt. 5+ fős forgatások, castingok és hasonlók 10.000.Ft / óra áron elérhetőek. Sminkeléshez fél áron bérelheted ki a studiót - ha a mi sminkeseinkkel dolgozol, akkor sminkelés idejére ingyenesen!" : "The studio can be rented outside work hours for 10.000. HUF / hour - drop us an email. 5+ participant filmings, castings and similar cost 10.000. HUF / hour. For make-up you can rent the studio for half price - or if you work with our make-up artists, you can use it for free!"}</p>
    <section class="booking-extras"><h2>{hu ? "Extrák" : "Extras"}</h2><p>{hu ? "Studiónkban kérhetsz standard, vagy akár olyan extrákat is, amit sehol máshol nem találsz Budapesten!" : "Any other furniture, prop or wearable in our studio is for FREE."}</p><div class="booking-extras__grid">{bookingExtras.map((item: string[]) => <article><h3>{item[0]}</h3><p>{item[1]}</p><strong>{item[2]}</strong></article>)}</div></section>
  </>
) : page.section === "faq" ? (
  <>
    <section class="hero"><p class="eyebrow">{hu ? "gyakori kérdések" : "frequently asked questions"}</p><h1>{page.h1}</h1><p class="hero-lead">{introFor(page)}</p></section>
    <section class="faq-list">{faqItemsFor(page).map(([question, answer]) => <details><summary>{question}</summary><p>{answer}</p></details>)}</section>
    <p class="audit-note">{hu ? "A csomagárakat, lemondási szabályokat és elérhetőségeket a forrásaudit után kell véglegesíteni." : "Package prices, cancellation rules and contact details must be finalized after the source audit."}</p>
  </>
) : page.section === "legal" ? (
  <>
    <section class="hero"><p class="eyebrow">{hu ? "jogi információ" : "legal information"}</p><h1>{page.h1}</h1><p class="hero-lead">{introFor(page)}</p></section>
    <section class="legal-copy"><h2>{hu ? "Adatkezelés" : "Data processing"}</h2><p>{hu ? "Az oldalon küldött üzeneteket és foglalási adatokat kizárólag a megkeresés megválaszolásához és a szolgáltatás teljesítéséhez használjuk." : "Messages and booking information submitted through the site are used to respond to enquiries and provide the requested service."}</p><h2>{hu ? "Kapcsolat" : "Contact"}</h2><p>{site.email}<br />{site.publicAddress}</p><p class="audit-note">{hu ? "A teljes jogi szöveg és a hivatalos szolgáltatói cím közzététel előtt jogi ellenőrzést igényel." : "The complete legal text and official provider address require legal review before publication."}</p></section>
  </>
) : page.path.endsWith("photographer-daniel") ? (
  <>
    <section class="hero"><p class="eyebrow">{hu ? "fotós profil" : "photographer profile"}</p><h1>{page.h1}</h1><p class="hero-lead">{hu ? "Dániel Z. Aczél fotós és az aether art space alapítója Budapesten." : "Dániel Z. Aczél is a photographer and the founder of aether art space in Budapest."}</p><div class="actions"><a class="button" href={`mailto:${site.email}`}>{hu ? "kapcsolat" : "contact the studio"}</a><a class="text-link" href={hu ? "/hu/photographers-budapest" : "/photographers-budapest"}>{hu ? "rezidens fotósok" : "resident photographers"}</a></div></section>
    <section class="split-copy"><div><h2>{hu ? "Fotózás, tanítás és közös stúdió" : "Photography, teaching and a shared studio"}</h2><p>{hu ? "Ezt a sitemapból is elérhető profilt a migráció alatt megőrizzük." : "This sitemap-discovered profile is preserved during migration."}</p></div><div class="audit-note">{page.action}</div></section>
  </>
) : (
  <>
    <section class="hero"><p class="eyebrow">{page.purpose}</p><h1>{page.h1}</h1><p class="hero-lead">{introFor(page)}</p><div class="actions"><a class="button" data-gtag-event="booking_click" data-gtag-location="service" href={site.bookingUrl}>{hu ? "érdekel a foglalás" : "check availability"}</a><a class="text-link" href={relatedHref}>{hu ? "csomagok megtekintése" : "view photography packages"}</a></div></section>
    <section class="split-copy"><div class="visual-card"><span>budapest</span><strong>aether<br />art space</strong></div><div><p class="eyebrow">{hu ? "a lehetőségek" : "made for the process"}</p><h2>{page.h1}</h2><p>{hu ? `Az ${page.purpose.toLowerCase()} szolgáltatást a budapesti aether art space-ben úgy alakítottuk ki, hogy az alkotáshoz szükséges tér és figyelem egy helyen legyen.` : `Our ${page.purpose.toLowerCase()} offering is built around a calm, flexible studio environment with the space and attention your project needs.`}</p><p>{hu ? "A pontos részletekért, elérhetőségért és személyre szabott ajánlatért írj nekünk." : "Contact us for details, availability and a tailored recommendation."}</p><a class="text-link" href={`mailto:${site.email}`}>{hu ? "írj nekünk" : "send us a message"}</a></div></section>
    <section class="related"><p class="eyebrow">{hu ? "kapcsolódó oldalak" : "continue exploring"}</p><div class="related-links">{serviceLinks.map(([path, label]) => <a class="text-link" href={path}>{label}</a>)}</div></section>
  </>
)}

<script>
  const lightbox = document.querySelector<HTMLDialogElement>("[data-studio-lightbox]");
  const image = lightbox?.querySelector<HTMLImageElement>("[data-lightbox-image]");
  const galleryItems = [...document.querySelectorAll<HTMLElement>("[data-gallery-image]")];
  let activeIndex = 0;

  const showImage = (index: number) => {
    const item = galleryItems[index];
    if (!item || !image) return;
    activeIndex = index;
    image.src = item.dataset.gallerySrc || "";
    image.alt = item.dataset.galleryAlt || "";
  };

  document.querySelectorAll<HTMLElement>("[data-model-card]").forEach((card) => {
    const main = card.querySelector<HTMLButtonElement>("[data-model-main]");
    const mainImage = card.querySelector<HTMLImageElement>("[data-model-main-image]");
    const thumbs = [...card.querySelectorAll<HTMLButtonElement>("[data-model-thumb]")];
    thumbs.forEach((thumb) => thumb.addEventListener("click", () => {
      if (!main || !mainImage) return;
      main.dataset.gallerySrc = thumb.dataset.modelSrc || "";
      main.dataset.galleryAlt = thumb.dataset.modelAlt || "";
      mainImage.src = thumb.dataset.modelSrc || "";
      mainImage.alt = thumb.dataset.modelAlt || "";
      mainImage.style.objectPosition = thumb.dataset.modelPosition || "50% 50%";
      thumbs.forEach((item) => {
        const active = item === thumb;
        item.classList.toggle("is-active", active);
        item.setAttribute("aria-pressed", String(active));
      });
    }));
  });

  galleryItems.forEach((item, index) => item.addEventListener("click", () => {
    if (!(lightbox instanceof HTMLDialogElement)) return;
    showImage(index);
    lightbox.showModal();
  }));

  lightbox?.querySelector("[data-lightbox-close]")?.addEventListener("click", () => lightbox.close());
  lightbox?.querySelector("[data-lightbox-previous]")?.addEventListener("click", () => showImage((activeIndex - 1 + galleryItems.length) % galleryItems.length));
  lightbox?.querySelector("[data-lightbox-next]")?.addEventListener("click", () => showImage((activeIndex + 1) % galleryItems.length));
  lightbox?.addEventListener("click", (event) => { if (event.target === lightbox) lightbox.close(); });
</script>
